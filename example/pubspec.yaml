name: example
description: Example project for constraint_layout

publish_to: 'none'

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # constraint_layout: ^1.0.0
  constraint_layout:
    path: ../../constraint_layout

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true